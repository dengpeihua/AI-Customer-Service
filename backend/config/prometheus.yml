# Salso 的 Prometheus 配置

global:
  scrape_interval: 15s
  evaluation_interval: 15s
  external_labels:
    cluster: 'salso'
    environment: 'production'

rule_files:
  - "alerts/*.yml"

scrape_configs:
  # 我们的主应用指标
  - job_name: 'salso-app'
    static_configs:
      - targets: ['salso:8000']
        labels:
          service: 'salso'
    scrape_interval: 10s
    metrics_path: '/metrics'

  # Prometheus 自监控
  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']
